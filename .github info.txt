---- Da fare ogni volta che cambio il progetto:
------- Il primo è per heroku, il secondo per git hub

git add .
git commit -m "messaggio qui"
git push heroku master

git push -u origin master

----


**** Struttura upload sito su server remoti o git hub ****

---- su heroku

git init
heroku git:remote -a glossario-gestisco 

heroku git:remote -a metaglossario-gestisco 

git add .
git commit -m "messaggio qui"

git push heroku master

---- su git hub

git init
git remote add origin https://github.com/tommasosansone91/basic-social-network.git

git add .
git commit -m "messaggio qui"

git push -u origin master

---- da github verso una cartella locale

vai nella repository github
download
download with https
copia link
anaconda> directory in cui vuoi copiare il progetto
git clone link

---- controlla le repository di heroku e git associate alla current directory

git remote -v

directory locale e URL della repository remota vengono associate grazie ai comandi

git init
git remote add origin https://github.com/myprofile/app1.git

(non so bene in che ordine)

-------

solo una origin può essere attiva

# cancellare l'origin

git remote rm origin



